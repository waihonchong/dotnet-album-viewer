pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building dotnet file'
            }
        }
        stage('Run') {
            steps {
                echo 'Running dotnet file'
            }
        }
		stage('Deploy') {
            steps {
                echo 'Simulating deploy'
            }
        }
    }
}
